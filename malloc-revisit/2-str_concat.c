#include "main.h"
#include <stdlib.h>

/**
 * str_concat - A function that concatenates two strings
 *
 * @s1: the first string
 * @s2: the second string
 *
 * Return: a pointer to the nwly concatenated string.
 */

char *str_concat(char *s1, char *s2)
{
	int i;
	char *new_str;

	if (*s1 == NULL)
		*s1 = "";
	if (*s2 == NULL)
		*s2 = "";
	new_str = malloc(sizeof(s1 + s2
		


}
